import React from 'react';
import s from './styles.css';

const SignUp = (props) => {
  console.log(props);

  return (
    <div className={s.form}>
      sign up container
    </div>
  );
};

export default SignUp;
